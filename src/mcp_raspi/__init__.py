"""
Raspberry Pi MCP Server - Non-privileged MCP server process.

This package implements the MCP protocol (HTTP/JSON-RPC), processes authentication,
validates parameters, enforces authorization, and routes requests to domain modules.
"""

__version__ = "0.1.0"
